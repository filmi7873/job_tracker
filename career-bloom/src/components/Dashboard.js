function Dashboard({ totalJobs }) {
    return (
      <div style={{ marginBottom: '20px' }}>
        <h2>Dashboard</h2>
        <p>You’ve added {totalJobs} jobs 🌱</p>
      </div>
    );
  }
  
  export default Dashboard;